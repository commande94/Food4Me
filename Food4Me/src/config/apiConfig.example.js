// Example API config — copy this file to `apiConfig.js` and update the URL for your environment.
export const API_URL = "http://localhost:3000";

// Usage:
// - Commit `apiConfig.example.js` to the repo.
// - Each developer should copy it to `src/config/apiConfig.js` and adjust the value (or use environment variables).
